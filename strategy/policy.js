const tensorFlow = require("tensorflow");
const tf = tensorFlow.tensor([1, 1]);

const actions = ["attack", "switch"];
console.log(tf);

// tf.resetDefaultGraph();
// const chosenAction = tf.max(weights)
